requests>=2.25.0
xmltodict>=0.12.0
# note: the solace_broker_service role uses community.docker.docker_compose_v2, which
# requires the Docker CLI with the Compose V2 plugin (docker compose) at the system
# level - not the End-of-Life 'docker-compose' (v1) Python package.
